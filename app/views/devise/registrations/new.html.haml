.devise-card
  .card
    .card-content
      .title.is-4=t('devise.registrations.new.sign_up')

      = simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
        = render "devise/shared/error_messages", resource: resource

        = f.input :avatar, as: :file, input_html: { class: "input" }

        = f.input :email, autofocus: true, autocomplete: "email", input_html: { class: "input", placeholder: "Email" }
        
        = f.input :first_name, autocomplete: "first_name", input_html: { class: "input", placeholder: "Nom" }
        
        = f.input :last_name, autocomplete: "last_name", input_html: { class: "input", placeholder: "Prénom" }
        
        - if @minimum_password_length
          %em= t('devise.shared.minimum_password_length', count: @minimum_password_length)
          %br/
        = f.input :password, autocomplete: "new-password", input_html: { class: "input", placeholder: "Mot de passe" }

        = f.input :password_confirmation, autocomplete: "new-password", input_html: { class: "input", placeholder: "Mot de passe" }

        .card-footer
          = f.submit t('devise.registrations.new.sign_up'), class: 'button has-background-link-light'

          = render "devise/shared/links"
 
          = link_to t("activerecord.back"), :back, class: "button"